{
  "private": true,
  "name": "gatsby-theme-documentation",
  "version": "0.0.1",
  "scripts": {
    "start": "yarn workspace gatsby-starter-documentation start",
    "build": "gatsby build",
    "clean": "gatsby clean",
    "docs": "gatsby develop",
    "now-build": "yarn build",
    "publish": "lerna publish",
    "publish:ci": "lerna publish -y --canary --preid ci --dist-tag=ci --force-publish=*",
    "test": "yarn build"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "dependencies": {
    "gatsby": "3.7.2",
    "lerna": "4.0.0",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "resolutions": {
    "react": "17.0.2",
    "react-dom": "17.0.2"

  }
}
