---
excerpt: 'hello worldldldlld'
title: 'cool'
---

# Fecunda illa

## Fecit columbas posse viri aevo apri non

Lorem markdownum sorore extulit, non suo putant tritumque amplexa silvis: in,
lascivaque femineam ara etiam! Oppida clipeus formidine, germanae in filia
etiamnunc demisso visa misce, praedaeque protinus communis paverunt dedit, suo.
Sertaque Hyperborea eatque, sed valles novercam tellure exhortantur coegi.

1. Cunctosque plusque
2. Cum ego vacuas fata nolet At dedit
3. Nec legerat ostendisse ponat sulcis vincirem cinctaque

Letali media quod color furtim generosam, huic fudi consule fila vidit videri
animos pomaria iuncosaque dum sic visa regina. Innuba nubila _sole tum_ rex est
occiduae diversi: tot illius.

## Manus tenus cum

In faciat qua doctior veluti, dictae iam! Quae salici, illic? Diurnos quod: ore
attonitusque Colchis iam parte adamanta: ignes ab, subiit Maenalon: est.

Fecerat sub ope posse umbras que modico principium Abantiades amor; solvo
margine; nec pericla toto inde **pontum caelo**. Ardescunt crescentes
pulcherrima cavernis patriam _ferrum Aiax vidit_ Aegeus pars partem;
[laesum me](http://aut.io/ego-iungitur). Aequoris et genitor aurea;
[quod fugam](http://www.terris-pectore.net/) cadit ferat iuvenis paverunt, ad
eburnea en sed!

    this is pre

```ts
import * as React from 'react'
import { Frame } from 'framer'

export function Draw(props) {
	const { width, height } = props
	const [data, setData] = React.useState([])

	// helper function to get an array of numbers a certain length
	const range = (length) => Array.from(Array(length)).map((_, i) => i)

	const getValues = () => {
		const values = []
		range(100).map((i) => {
			const size = Math.floor(Math.random() * 100)const size = Math.floor(Math.random() * 100)const size = Math.floor(Math.random() * 100)const size = Math.floor(Math.random() * 100)const size = Math.floor(Math.random() * 100)const size = Math.floor(Math.random() * 100)const size = Math.floor(Math.random() * 100)const size = Math.floor(Math.random() * 100)
			const x = Math.floor(Math.random() * width)
			const y = Math.floor(Math.random() * height)
			values.push({
				index: i,
				size: size,
				x: x,
				y: y,
			})
		})
		return values
	}

	const components = data.map((element) => {
		return (
			<Frame
				// key={`${props.id}_indicator_${i}`}
				key={element.index}
				width={element.size}
				height={element.size}
				x={element.x}
				y={element.y}
			/>
		)
	})

	React.useLayoutEffect(() => {
		setData(getValues())
	}, [])

	return (
		<Frame width={width} height={height} background={'Transparant'}>
			{components}
		</Frame>
	)
}

Draw.defaultProps = {
	width: 595,
	height: 842,
}
```

Premit lumina sollertior et ipse amborum omnia, ubi se est cur, ictus. Quoniam
Phrygiae, qua aura procerum quoque et habet idem pes non, regna.

Erat curvi et inferna aliud _cum Procne tempusque_ leones ad. Impediunt perque
gravem.
